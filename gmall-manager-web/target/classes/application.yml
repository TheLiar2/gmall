server:
  port: 9100
spring:
  dubbo:
    application: gmall-manager-web
    consumer:
      check: false
      timeout: 60000
    registry:
      address: 192.168.10.100:2181
      protocol: zookeeper
    base-package: com.gmall.controller
#logging:
#  level:
#    root: debug
